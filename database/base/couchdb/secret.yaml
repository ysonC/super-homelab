apiVersion: v1
kind: Secret
metadata:
  name: couchdb-couchdb
  namespace: database
stringData:
  adminUsername: "admin"
  adminPassword: "super-password"
  cookieAuthSecret: "random-cookie-secret"
  erlangCookie: "random-cookie-secret"
