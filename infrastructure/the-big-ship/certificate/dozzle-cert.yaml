apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dozzle-ingress-certificate
  namespace: monitoring
spec:
  secretName: dozzle-certificate-secret
  issuerRef:
    name: cloudflare-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - dozzle.ysonsbaolab.dev
