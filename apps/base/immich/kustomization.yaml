apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - db-url-secret.yaml
  - helmrelease.yaml
  - repo.yaml
  - pvc.yaml
  - ingress.yaml
  - hpa.yaml
  - microservices-deployment.yaml
# configMapGenerator:
#   - name: immich-values
#     namespace: apps
#     files:
#       - values.yaml
# generatorOptions:
#   disableNameSuffixHash: true
