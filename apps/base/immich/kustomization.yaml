apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - repo.yaml
  - db-url-secret.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: immich-values
    namespace: apps
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
